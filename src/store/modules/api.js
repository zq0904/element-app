// 代理
let p1 = '/proxy'

// 接口地址
const state = {
  seller: p1 + '/seller', // 卖方信息
  goods: p1 + '/goods', // 商品信息
  ratings: p1 + '/ratings' // 平级信息
}
const actions = {}
const mutations = {}

export default {
  state,
  actions,
  mutations
}
